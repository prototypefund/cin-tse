"""Tests for the BNN backends."""
# import socket


#class TestTSE:
    #def test_atmp(self, tse):

        #print(tse.host)
        #print(tse.ssl)


        # s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        # s.connect(('10.0.0.2', 8009))
        # antwort = s.recv(1024)
        #
        # print("{}".format(antwort.decode()))
